#include <iostream>
#include <fstream>

using namespace std;

int main(){
    ifstream fin("cifre4.in");
    ofstream fout("cifre4.out");

    int nr, n, p, x, val;
    fin >> n;
/*
    while (fin >> n >> p){
        x = 0;
        while (true){
            val = x * p + n;
            if (verif(val))
                cout << val << endl;
        }
    }
*/
}